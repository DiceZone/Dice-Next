// ─── Dice!Next Test Suite — Main Entry Point ──────────────────
// Runs all registered test cases and returns exit code.

#include "test_framework.h"

int main() {
    return dice::test::runAll();
}
